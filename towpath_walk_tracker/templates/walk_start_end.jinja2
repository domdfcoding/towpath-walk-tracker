{# djlint:off #}
{% macro script(this, kwargs) %}

    function placeMarkerOnClick(e) {
        if (sidebar_add_walk_pane.classList.contains("active")) {
            placeMarkerOnMap(e.latlng, walkPreview, walkForm);
        }
    }

    map_canal_towpath_walking.on('click', placeMarkerOnClick);
{% endmacro %}
{# djlint:on #}
